const settings = {
	output: 'export',
	experimental: {
		outputStandalone: true,
	},

	devIndicators: {
		buildActivity: false,
	},
	swcMinify: true,
	staticPageGenerationTimeout: 60,
}

module.exports = settings
